import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'suitefactor_front',
  webDir: 'dist',
  server: {
    androidScheme: 'http'
  }
};

export default config;
